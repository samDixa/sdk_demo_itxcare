<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Outer Shadow Layer -->
    <item>
        <shape android:shape="oval">
            <solid android:color="#B0BEC5"/> <!-- Outer grey shadow color -->
        </shape>
    </item>

    <!-- Inner Main Circle -->
    <item android:drawable="@drawable/inner_circle"
        android:top="4dp"
        android:bottom="4dp"
        android:left="4dp"
        android:right="4dp"/>
</layer-list>
